import fs from 'node:fs';import path from 'node:path';import {CONTENT,readJson,listFiles} from './lib.mjs';for(const k of ['blog','classes']){const d=path.join(CONTENT,k);if(!fs.existsSync(d))continue;for(const e of fs.readdirSync(d,{withFileTypes:true})){if(!e.isDirectory())continue;const p=path.join(d,e.name),m=path.join(p,'meta.json');if(fs.existsSync(m))readJson(m,null);if(!listFiles(p).length)throw Error(k+'/'+e.name+' is empty')}}console.log('Content check passed.');
